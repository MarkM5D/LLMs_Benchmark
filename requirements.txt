# Core Dependencies (automatically installable)
torch>=2.0.0
torchvision
torchaudio
transformers>=4.30.0
accelerate
datasets
huggingface_hub

# Analysis and Visualization
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
tqdm>=4.60.0

# System Monitoring
psutil>=5.8.0
nvidia-ml-py3
gpustat

# Note: Inference engines (vllm, sglang, tensorrt-llm) are installed
# separately by setup_env.sh due to complex CUDA dependencies